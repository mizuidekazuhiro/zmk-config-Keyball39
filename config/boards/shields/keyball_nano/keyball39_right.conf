# =====================
# Mouse / Trackball
# =====================
CONFIG_ZMK_MOUSE=y

# =====================
# BLE（split + mouse 安定）
# =====================
CONFIG_BT_BUF_EVT_RX_COUNT=16
CONFIG_BT_BUF_ACL_TX_COUNT=32
CONFIG_BT_L2CAP_TX_BUF_COUNT=32

# NOTE: Removed CONFIG_BT_CTLR_TX_BUFFERS because symbol is undefined in this build (causes Kconfig warnings).
CONFIG_BT_CTLR_RX_BUFFERS=16
